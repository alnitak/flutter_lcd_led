name: lcd_led
description: "Experience the nostalgic charm of a 7-segment LED display with accurate rendering of digits, commas, and dots."
version: 1.0.0
homepage: https://github.com/alnitak/flutter_lcd_led
issue_tracker: https://github.com/alnitak/flutter_lcd_led/issues
repository: https://github.com/alnitak/flutter_lcd_led

screenshots:
  - description: "LCD LEDs"
    path: images/screenshot.png

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

